using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DataModel;

public class DeviceCreationTest : MonoBehaviour
{
    void Start()
    {
        //ButtonProperty bp = new ButtonProperty(ButtonState.Off);
        //DeviceElement el1 = new DeviceElement(new IProperty[] { bp });

        //for (int i = 0; i < el1.Properties.Length; i++)
        //{
        //    if (el1.Properties[i] is ButtonProperty)
        //    {
        //        var bpLocal = el1.Properties[i] as ButtonProperty;
        //        Debug.Log(bpLocal.ButtonState);
        //        bpLocal.Switch();
        //        Debug.Log(bpLocal.ButtonState);
        //    }
        //}
    }
}
